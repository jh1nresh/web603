# Week 6 Day 1 Homework — Travel App

The homework's single-page React Redux travel planner. All child components,
actions, reducer, store, and React Redux connection live in `src/App.js`, as the
slides request.

## Run

```bash
npm install
npm start
```

## Verify

```bash
npm test -- --watchAll=false
npm run build
```
