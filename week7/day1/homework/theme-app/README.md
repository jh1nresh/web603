# Week 7 Day 1 Homework — Theme App

A React/Redux app that uses `styled-components` and `styled-theming` to switch
between light and dark themes.

```bash
npm install
NODE_OPTIONS=--openssl-legacy-provider npm start
```

The `NODE_OPTIONS` flag lets the assignment's supplied-generation CRA tooling
run on current Node versions.
