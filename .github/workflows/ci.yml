name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  check:
    uses: jh1nresh/.github/.github/workflows/web-ci.yml@0cb6fd5b730ced897c767d1c9979aba4e49d201d
    with:
      node-version: "22"
      working-directory: week4/day3/coursework-app
      lockfile: week4/day3/coursework-app/package-lock.json
      verify-script: check
      output-directory: dist
      audit-production: false
      timeout-minutes: 15
      job-key: web603-coursework
